# トラブル記録テンプレート

用途: 今起きているトラブルを記録し、コーディングAIにタスクリストとして与えるためのフォーマット。

---
## 基本情報
- タイトル: 共有ページで音素材の再生に失敗する。

## 概要
- 
```
[Error] Play audio error: – NotSupportedError: The operation is not supported.
NotSupportedError: The operation is not supported.
	（anonymous関数） (main.deca6e69.js:2:263940)
	```

---
## 基本情報
- タイトル: これまでのエラーの修正方法
---

## 次のステップ：アプリの「体質改善」で、さらなる安定動作を目指す

コーディングAIによる修正は、問題の「症状」を見事に抑え込みました。ここからは、その症状が二度と現れないように、問題の「原因」そのものを取り除くための改善に進みましょう。

### 提案1：ドラッグ＆ドロップを「ID方式」に刷新し、データ欠損を完全に防ぐ

**現状の課題**
AIの修正により、ドロップ時にデータが欠けていても `sounds` ステートから情報を補うことでエラーを防ぐようになりました。これは素晴らしい改善ですが、「ドラッグ中にデータが欠けることがある」という根本的な不安定さは残っています。

**解決策**
ドラッグ＆ドロップで受け渡す情報を、音素材の**ユニークIDのみ**にする方式に変更します。

**具体的な指示（コーディングAI向け）**

1.  **ドラッグ開始処理の変更 (`SoundLibrary.js`, `DAWPage.js`)**
    * `onDragStart` イベントで `e.dataTransfer.setData()` に渡すデータを、音素材オブジェクト全体（JSON）から、**音素材のID（文字列）のみ**に変更してください。
    * 例： `e.dataTransfer.setData('text/plain', sound.id);`

2.  **ドロップ処理の変更 (`DAWPage.js`)**
    * `handleDrop` 関数で、`e.dataTransfer.getData()` を使って**音素材のIDを受け取ります**。
    * そのIDを元に、`DAWPage` が保持している `sounds` ステート（全ての音素材リスト）から、**完全な音素材オブジェクトを検索**します。
    * 検索して見つかった、**信頼できる完全なデータ**を使って、タイムラインに新しいクリップを追加してください。

**この改善によるメリット**
* ドラッグ中にデータが壊れたり失われたりする心配が**完全になくなります**。
* 受け渡すデータ量が最小限になるため、アプリの**パフォーマンスが向上**します。
* 「音素材データが見つかりません」というエラーの**根本原因を断ち切る**ことができます。

---

### 提案2：音声再生を「Web Audio API」に統一し、再生の専門家を一人に絞る

**現状の課題**
現在、音楽づくり(DAW)ページでは高度な「Web Audio API」を、音ライブラリなど他のページでは簡易的な「HTMLの`<audio>`タグ」を使っており、二つの再生方式が混在しています。これが原因で、iPadのような特定の環境で再生の挙動に差が出てしまいます。

**解決策**
アプリ内のすべての音声再生を、より高機能で安定している **Web Audio API** に統一します。

**具体的な指示（コーディングAI向け）**

1.  **共有の `AudioContext` を作成する**
    * アプリの最上位コンポーネント（ `App.js` など）で `AudioContext` を一度だけ生成し、React Contextを使ってアプリ全体で共有する仕組みを作ってください。
    * ユーザーが最初に画面を操作したときに、この共有 `AudioContext` を `resume()` させる処理を確実に入れてください。

2.  **音声再生機能を共通化する**
    * `SoundLibrary.js` や `SoundCollection.js` などで使われている `<audio>` タグをすべて削除します。
    * 代わりに、共有された `AudioContext` を使って音声を再生するための、**共通の関数やカスタムフック**（例： `useAudioPlayer` ）を作成してください。
    * プレビュー再生はすべて、この共通関数を使って行うように修正します。

3.  **音声データを再生に適した形式でキャッシュする**
    * アプリ起動時に、localStorageから読み込んだ音声データ（Base64形式）を、Web Audio APIが最も扱いやすい **`AudioBuffer`** という形式に一度だけ変換します。
    * 変換した `AudioBuffer` を、音素材のIDと紐づけてメモリ上に保持（キャッシュ）しておきます。
    * 音を再生する際は、毎回データを変換するのではなく、キャッシュから `AudioBuffer` を直接取り出して再生するようにしてください。

**この改善によるメリット**
* **再生の挙動がアプリ全体で統一**され、ページによって音が鳴ったり鳴らなかったりする問題が解消されます。
* iPadOS/iOS Safariで特に厳格な**音声再生ポリシーに、より確実に対応**できます。
* 音声データを事前に最適な形式に変換しておくことで、**再生時のパフォーマンスが大幅に向上**します。

---

これらの「体質改善」を行うことで、あなたのアプリは格段に堅牢で信頼性の高いものになります。
まずはこの2つの提案をコーディングAIに伝えてみてください。