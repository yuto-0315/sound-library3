# 安定化・データ消失・iPad 互換性の修正 作業記録

| 項目 | 内容 |
|:--|:--|
| 作業日時 | 2026-09-24（木）00:00 ごろ 〜 02:20 ごろ（JST） |
| ブランチ | `fix/stability-and-data-loss`（`main` の `d26bc9a0` から分岐） |
| 作業者 | Claude Code（依頼者: yut-0315） |
| 対象 | フロントエンド（`src/`）、PHP API（`api/`）、テスト、README |

---

## 1. 依頼内容と報告されていた症状

- 動作が安定しない
- 一部の iPad では使用できない
- データが飛んでしまう（致命的）
- 音楽づくりページから音あつめページへ移動して戻ると、**タイムラインの内容が全部消える**
- あわせて依頼されたこと: テストの追加（多めに）、必要に応じたリファクタリング、UI の不都合の修正、アイコンを **lucide-react** に変更、`node_modules` をコミットから外す、作業記録の作成、PR の作成

---

## 2. 調査のしかた

1. すべてのページ（`SoundCollection` / `SoundLibrary` / `DAWPage` / `CloudPage` / `AdminPage`）、データ層（`utils/indexedDB.js`）、PHP API を全文読んで、データの流れ（録音 → 保存 → 読み込み → DAW → 自動保存 → クラウド）を追った
2. 過去の修正記録（`iPad音声再生問題の修正.md`、`トラブル.md` など）の症状（`NotSupportedError`、特定の iPad だけ無音）と照らし合わせた
3. 修正前のテストを実行してベースラインを記録した（92 件中 48 件成功・44 件失敗）
4. 修正後は Jest の単体・結合テスト、Chromium での E2E テスト、PHP の CLI テストで確認した
5. 独立したレビュアー（サブエージェント）によるレビューを **2 周** 行い、指摘を修正した（→ 6 章）

---

## 3. 見つかった原因と修正

### 3.1 データが消える問題

| # | 症状 | 原因 | 修正 |
|:--|:--|:--|:--|
| D1 | **ページを移動して戻るとタイムラインが消える**（報告された症状） | DAW を開いた直後、保存済みの作業内容を非同期で読み込んでいる**間に**、自動保存の effect が初期状態（空のトラック 1 つ）を `localStorage` に書き込んでいた。その後の読み込みでは、今書かれた空のデータが読まれていた | 読み込みが終わるまで自動保存しない（`isProjectLoaded`）。読み込みに失敗した場合も、空の状態で上書きしないよう自動保存を止める |
| D2 | 音をたくさん置くと、再読み込みで作業内容が消える | 自動保存先が `localStorage`（約 5MB）で、クリップごとに音声データ（base64）を丸ごと保存していたため、すぐ容量を超えて保存に失敗していた（エラーはコンソールに出るだけ） | 自動保存を **IndexedDB** に移動。音声データは 1 回だけ保存する形式（v2）にした。旧データは自動で移行 |
| D3 | 先生ページから楽曲を開くと、もともとあった音素材が消える | 取り込んだ音素材に「既存の最大 ID + 1」を振っていたが、読み込みが終わる前は既存一覧が空のため ID が 1, 2… になり、**既存の録音を上書きしていた** | ID は IndexedDB に自動採番させる（`addRecording` / `addRecordings`） |
| D4 | 保存したはずの録音が消えていることがある | IndexedDB の書き込みを、リクエストの `onsuccess` で成功扱いにしていた。実際の書き込み確定（`complete`）前なので、容量不足などで中断されても「保存済み」に見えていた | すべての処理をトランザクションの `complete` まで待ってから成功を返す（`runTransaction`） |
| D5 | 旧データの移行で録音が消える | 移行時に「名前が同じなら同じ録音」と判定していたため、同じ名前の別の録音（例: たいこ が 2 つ）は片方が捨てられ、その後 localStorage も消されていた | 名前・作成日時・音声データで判定し、全件の書き込み確定後に、移行した分だけ localStorage から消す |
| D6 | 共有ページで「音ライブラリーに追加」すると失敗しやすい | ダウンロードした音声を容量の小さい localStorage に保存していた | IndexedDB に保存。同じ音声の二重追加も防止 |
| D7 | 録音一覧の音が途中で再生できなくなる | 後片付けの effect が「録音一覧が変わるたび」に実行され、表示中の音の URL まで解放していた | 後片付けはアンマウント時だけにした |
| D8 | 読み込み失敗後に「リセット」で保存データが消える（レビューで発見） | 失敗時の案内が「リセットを押して」となっており、リセットは読めなかったデータを削除していた | 「もう一度読み込む」ボタンと、閉じられない「自動保存停止中」表示を追加。リセットは作業内容をバックアップしてから行い、バックアップできなければ中止する |
| D9 | 先生ページから開いた楽曲で、端末の作業内容が黙って置き換わる（レビューで発見） | 確認なしで置き換え、しかも「取り込み → 削除 → 自動保存」が別々の処理だった | 作業内容があれば確認。「バックアップ → 自動保存に書く → 取り込み元を削除」を 1 つのトランザクションで行う |
| D10 | 名前を付ける前に別のページへ行くと、録音が消える（レビューで発見） | 保存前の録音はページの state にしか無かった | アプリを開いている間は一時的に覚えておき、戻ったときに復元する（`utils/unsavedDraft.js`）。新しく録音するときは確認する |
| D11 | 自動保存に 1 回失敗した後、何もせず移動すると保存されない（レビューで発見） | 失敗時に「保存待ち」の印を戻していなかった | 失敗時は保存待ちに戻し、ページを離れるときに再保存する |
| D12 | DAW を 2 つのタブで開くと、古いタブが新しい内容を上書きする（レビューで発見） | タブに戻ったときに作業内容を読み直していなかった | タブに戻ったとき、より新しい保存があれば読み込み直す |
| D13 | Safari がしばらく使わないサイトのデータを消す | Safari は一定期間操作されないサイトの保存データを削除することがある | 起動時に `navigator.storage.persist()` で永続化を依頼 |
| D14 | リセットや先生の楽曲で置き換えた作業内容を戻せない（2 周目で発見） | バックアップは取っていたが、画面から戻す手段が無く、空の作業内容で上書きされることもあった | 「1つ前の作業に戻す」ボタンを追加（押すたびに入れ替わる）。クリップのある作業内容だけをバックアップする |
| D15 | プロジェクトファイルを読み込むと、今の作業内容が確認なしに置き換わる（2 周目で発見） | 読み込み失敗で自動保存が止まっている場合も、読めなかったデータを上書きしていた | 確認し、バックアップしてから置き換える |
| D16 | 旧バージョンの作業内容（localStorage）しか無い端末で、先生の楽曲を開くと旧データが消える（2 周目で発見） | 「今の作業内容があるか」を IndexedDB だけで判定し、その後の保存で旧データを削除していた | 旧データも作業内容として扱って確認・バックアップし、移行かバックアップが済むまで消さない |
| D17 | 別タブの内容を読み込み直している間の編集が消える（2 周目で発見） | 読み込みを待つ前後で、このタブで編集したかを確認していなかった | 読み込みの前後で編集が無いことを確認してから置き換える |
| D18 | 保存中に次の録音をして、先の保存が失敗するとその音が消える／保存中にページを離れて失敗すると消える（2 周目で発見） | 保存中でも次の録音ができ、失敗時に一時保管していなかった | 保存中は録音・ファイル選択を無効化。失敗時はページを離れていても一時保管し、入力途中の名前・タグも戻す |

### 3.2 一部の iPad で使えない・動作が不安定な問題

| # | 症状 | 原因 | 修正 |
|:--|:--|:--|:--|
| I1 | iPad で録音した音が再生できない（`NotSupportedError`） | iPad の録音は mp4 なのに、Blob・Data URL・アップロード時のファイル名をすべて **audio/wav** と表記していた | 実際の録音形式で保存。**中身（先頭バイト）から形式を判定**して表記を直す処理を追加し、保存済みの誤ったデータも読み込み時に直す |
| I2 | PC で録音して共有した音が古い iPad で鳴らない | Chrome は `audio/mp4` だけ指定すると中身が Opus になる | 録音形式の第一候補を `audio/mp4;codecs=mp4a.40.2`（AAC）にした |
| I3 | 古い iPad（iPadOS 14.0 以前）で DAW の音が出ない・長さが取れない | `Blob.arrayBuffer()`、Promise 版 `decodeAudioData`、`new AudioContext({ sampleRate })` など古い Safari に無い API を使っていた | FileReader による代替、コールバック版の併用、オプション無しの AudioContext に変更 |
| I4 | iPad で再生のタイミングがずれる・停止後に鳴る・鳴らない | クリップごとに `<audio>` を作って `setTimeout` で再生していた（読み込み待ちで遅れる、停止しても待ち中の音が後から鳴る、iOS の自動再生制限にかかる） | **Web Audio API** で事前にデコードし、AudioContext の時計で予約する方式に変更。停止・移動時は予約中の音も確実に止める |
| I5 | 消音スイッチ（マナーモード）がオンの iPad で DAW が無音 | Web Audio は消音スイッチに従う | iOS 17 以降は `audioSession`、それ以前は無音の `<audio>` をループ再生して回避。※最初の実装では開始直後に自分で解除してしまう不具合があり、レビューで発見して修正 |
| I6 | 一部の iPad で IndexedDB を開けず、読み込み中のまま止まる | Safari 14.1〜15 の「読み込み直後の `indexedDB.open` が応答しない」不具合、ほかのタブによるアップグレードのブロック | `indexedDB.databases()` を使った回避策、タイムアウト、`onversionchange` での接続解放を追加 |
| I7 | 古い iPad で名前やタグを入力できない | タッチ端末向けの CSS が**全ページの入力欄にも** `user-select: none` をかけていた（本番ビルドでは全 CSS が 1 つにまとまるため、DAW ページ用の指定が全ページに効いていた） | DAW ページ内だけに限定し、入力欄は選択可能に戻した |
| I8 | タッチでドラッグするとページが先頭に飛ぶ・画面がスクロールできなくなる | ドラッグ中に `body` を `position: fixed` にしていた。`touchcancel` の処理が無く、割り込まれると固定されたままになっていた | `position: fixed` をやめ、`passive: false` のリスナーで `preventDefault` してスクロールを止める（`hooks/useTouchDrag.js`）。`touchcancel` も処理 |
| I9 | 縦向きの狭い iPad で音素材リストをスクロールできない（レビューで発見） | 幅 768px 以下ではリストが横並びになるのに、横方向の動きをドラッグとして扱っていた | 横並びのときは横をスクロール、縦（タイムラインの方向）をドラッグにする。音素材に「つまみ」を付け、つまみからはどの方向にもドラッグできる |
| I10 | 再生中に全体がカクつく | 再生中、毎フレーム state を更新して画面全体を描き直していた | プレイヘッドは DOM を直接動かし、React の再描画を通さない |
| I11 | 先生ページの「DAWで開く」が iPad で開かない | データの取得を待ってから `window.open` していたため、ポップアップとしてブロックされていた | クリック直後に空のタブを開いておき、準備ができたらそのタブで表示する |
| I12 | 共有ページの音声を iPad で再生できない | 取得を待ってから `play()` していたため、タップの扱いが切れて再生が拒否されていた | タップ直後に `<audio>` 要素の再生許可を取り、その要素を使い回す |
| I13 | 先生ページの音声プレーヤーが iPad Safari で再生できない | `download.php` が Range リクエストに対応していなかった（Safari の `<audio>` は対応していないサーバーの音声を再生しない） | 206 / 416 / HEAD に対応。保存済みファイルの中身から Content-Type を判定 |
| I14 | エラーが表示されてもすぐ消える | `useAccessibility` のエラー表示領域が、描画のたびに別のコンポーネントとして作り直されていた | コンポーネントの同一性を保つようにした |
| I15 | 共有ページで録音した音が一覧に出ない | 共有ページだけ、移行済みで空の localStorage を見ていた | IndexedDB から読むようにした |
| I16 | PHP 8.1 未満のサーバーで部屋に入れない | 部屋番号が文字列で返るのに、数値と `===` で比較していた | 型をそろえて比較（`utils/api.js`） |
| I17 | 予期しないエラーで画面が真っ白になる | エラー境界が無かった | `ErrorBoundary` を追加（ページ移動で元に戻る） |
| I18 | 電話や Siri の後で「再生中」のまま音が出ない（レビューで発見） | 中断後の `resume()` がいつまでも終わらないことがある | 1.5 秒で打ち切り、もう一度押すよう案内する |
| I19 | iPadOS 12 で保存・出力するとアプリのページから移動してしまう（レビューで発見） | download 属性が iOS 13 から | 非対応の場合は保存せずに案内する（重い合成処理を始める前に確認） |
| I20 | 短いクリップをドラッグしようとすると消えてしまう（2 周目で発見） | × ボタンの押せる範囲（44px）を広げていたため、短いクリップではほぼ全体が削除ボタンになっていた | クリップ内の × は押せる範囲を広げない |
| I21 | 2 本目の指を離すとドラッグが途中で確定する（2 周目で発見） | どの指が離れたかを見ていなかった | 最初に触れた指だけを追う |
| I22 | 音素材が多いと古い iPad でメモリが足りなくなる（2 周目で発見） | 共有ページ・音ライブラリで全音声を先読みし、取り込み時に使わない音声まで復元していた | 先読みしない（`preload="none"`）。必要な音声だけ復元する |

### 3.3 セキュリティ

| # | 問題 | 修正 |
|:--|:--|:--|
| S1 | 音声アップロードで、保存時の拡張子を送られてきたファイル名から取っていた。音声の MIME を名乗った `.php` ファイルを `api/uploads/audio/` に置けてしまう | 拡張子は中身から判定した形式で決める。`uploads/audio/.htaccess` で直接アクセスを禁止（配信は `download.php` 経由） |
| S2 | `download.php` が DB の `file_path` をそのまま連結していた | `basename()` を通して uploads の外を読ませない |

### 3.4 UI の不都合の修正

- 絵文字のアイコンを **lucide-react** に置き換え（`components/Icon.js`）。読み上げ用の名前（`role="img"` + `aria-label`）は従来どおり残した。CSS の疑似要素の絵文字も lucide の SVG に
- 音ライブラリのカードに「DAWページにドラッグ&ドロップできます」と表示されていたが、別のページなので実際にはできなかった → 「音楽づくりページの『音素材』から使えます」とリンク付きで案内
- 押しにくい小さなボタン（タグ編集・削除、タグの ×）を 44px 以上で押せるように（クリップの × は、短いクリップ全体が削除ボタンにならないよう広げない）
- `.error-message` が 4 つの CSS で別々にグローバル定義され、ページによって見た目が変わっていた／`max-height: 60px` で長いエラーが途中で切れていた
- iPad のマイク許可の案内を実際の設定の場所に修正
- 「トラック追加」の文字が途中で改行されないように、`100dvh` で iPad Safari の下端が見切れないように
- 自動保存の状況（保存待ち・保存中・保存しました・失敗・停止中）を表示
- 読み込み中は DAW を操作できないようにした（読み込み完了時に上書きされて消えるのを防ぐ）
- クリップをタイムラインの外で離すと確認なしで削除していた → 元の位置に戻るだけに（削除は × ボタン）
- 音が置かれたトラックを削除するときは確認する
- ナビゲーションが ARIA の menubar を名乗っているのに矢印キーで移動できなかった → roving tabindex を実装
- 「使い方」の開閉がキーボードでは二重に切り替わって開かなかった
- クラウド保存ダイアログ: 読み上げ用の名前、Esc で閉じる、開いたら入力欄にフォーカス、エラーをダイアログ内にも表示

---

## 4. データの保存形式・保存先の変更

### IndexedDB（`SoundLibraryDB`、バージョン 2 のまま）

| ストア | キー | 内容 |
|:--|:--|:--|
| `recordings` | 自動採番 | 録音した音（`audioData` は中身に合った MIME の Data URL） |
| `songs` | `daw-autosave` | DAW の自動保存（**新規**。以前は localStorage の `dawProjectAutoSave`） |
| `songs` | `daw-autosave-backup` | リセット・楽曲の取り込み・ファイル読み込み前の作業内容（**新規**。1 つだけ残す。「1つ前の作業に戻す」で入れ替え） |
| `songs` | `daw-import-song` | 先生ページから DAW へ渡す楽曲（従来どおり） |

- スキーマのバージョンは変えていないので、既存の端末のデータはそのまま使える
- 旧形式の localStorage（`soundRecordings`、`dawProjectAutoSave`、`daw-import-song`）は自動で移行し、移行が確定してから消す

### プロジェクト（タイムライン）の形式 v2（`utils/project.js`）

- v1: 各クリップの `soundData.audioData` に音声を丸ごと埋め込んでいた（同じ音を 10 回置くと 10 回分）
- v2: 音声は `assets` に 1 回だけ保存し、クリップは `audioRef` で参照する
- 読み込みは v1 / v2 の両方に対応（先生ページに保存済みの過去の楽曲も開ける）
- 自動保存・クラウド保存は「使っている音だけ」、プロジェクトファイルは「音素材ライブラリも含めて」保存する

### バックアップの復元

音楽づくりページの「1つ前の作業に戻す」ボタン（バックアップがあるときだけ表示）で、今の作業内容とバックアップを入れ替える。もう一度押すと元に戻る。バックアップは 1 つだけで、クリップのある作業内容を置き換えたときにだけ更新される。

---

## 5. テスト

| | 作業前 | 作業後 |
|:--|:--|:--|
| Jest | 92 件中 48 件成功・**44 件失敗** | 20 スイート **410 件すべて成功**（複数回連続で確認） |
| E2E（Playwright / Chromium） | 既存のスモークテストのみ | **12 件成功**（新規） |
| PHP | なし | **24 件成功**（新規） |
| ESLint（`npm run lint`） | テストファイルで多数のエラー | **エラー・警告なし** |

### 追加・書き直したもの

- `src/test-utils/fakeIndexedDB.js`: テスト用のインメモリ IndexedDB。トランザクションの complete / abort、書き込み確定の失敗（容量不足）、接続失敗・遅延、終わらないトランザクションを再現でき、範囲が重なる書き込みは本物と同じく順番に実行する
- `src/test-utils/audioFixtures.js`: 各音声形式のヘッダー、Blob を実際に読む FileReader、Web Audio のモック
- `DAWPage.persistence.test.js`: ページ移動・再読み込みでタイムラインが消えないこと、移動直前の変更も保存されること、読み込み失敗時に上書き・削除しないこと、取り込み時の確認とバックアップ、別タブの反映 など
- `DAWPage.interaction.test.js`: 再生の予約・一時停止・停止後に鳴らないこと、消音スイッチ対策、ID 方式の D&D、タッチドラッグ（スクロールとの区別、2 本目の指、touchcancel）、ズーム、クラウド保存、WAV 出力
- `SoundCollection.recording.test.js`: 録音形式、URL の解放、保存中の操作、保存前の録音の復元
- `SoundLibrary.data.test.js` / `CloudPage.test.js` / `AdminPage.test.js` / `stability.misc.test.js`
- `utils/`: 形式判定、Data URL、v1/v2 形式、移行、トランザクション確定、同時実行
- 既存の `SoundLibrary.test.js` / `SoundCollection.test.js` / `DAWPage.test.js` は、アプリに無い機能（テンポ/BPM、キーボードショートカット、ファイルのドラッグ枠など）や未インストールの API を前提にしていて一度も通っていなかったため、同じテスト意図を実在する機能で確かめる内容に書き直した
- `tests/playwright/stability.spec.js`: 本物の IndexedDB・MediaRecorder・Web Audio で確認（マイクの代わりに 440Hz の音を録音）
- `tests/php/audio_utils_test.php`: 形式判定と Range の解釈

### 実行方法

```bash
npm test -- --watchAll=false
php tests/php/audio_utils_test.php
npm run build && npm run start:static   # 別ターミナルで ↓
npx playwright test --config=playwright.config.js stability.spec.js
```

---

## 6. レビューの経緯（2 周）

### 1 周目（観点別に 3 つの独立レビュー）

- **React の競合状態・UI**: 消音スイッチ対策が再生開始直後に解除される（重大）、保存中のキャンセル、「使い方」のキーボード操作、読み込み中ロックがキーボードに効かない、録音中表示が残る
- **データ消失**: 読み込み失敗後のリセットで保存データを削除（重大）、自動保存失敗後の再保存、取り込みの確認と一括確定、保存前の録音、移行の重複、トランザクションのタイムアウト、複数タブ
- **iPad 互換性**: 縦向きの狭い iPad で音素材リストがスクロールできない（重大）、中断後の `resume()`、iPadOS 12 のダウンロード、2 本目の指、ドラッグ中の再描画、iPadOS 12 の CSS、PHP の Range

→ すべて修正し、それぞれ回帰テストを追加した（コミット `be200eb1`、`52c255e9`、`117bccf6`）

### 2 周目（1 周目の修正の検証 ＋ ブランチ全体の再レビュー）

- **検証**: 1 周目の 18 項目のうち大半は「正しく直っている」と確認。新たに、別タブ読み込み中の編集が消える競合、読み込み失敗中のファイル読み込みでの上書き、旧データのバックアップ漏れ、タイムアウトした処理が実は書き込まれている場合、2 本目の指を離したときの確定、保存中の録音まわり、開発時の URL 無効化を指摘
- **全体の再レビュー**: 重大な問題は無し。バックアップを戻せない、空の作業内容でバックアップが上書きされる、短いクリップの × で誤って削除、古い iPad でのメモリ負荷 などを指摘

→ すべて修正し、回帰テストを追加した（コミット `4002c093`）。最後にこのコミットだけを対象にした確認をもう一度行った（→ 下記）

### 最終確認（2 周目の修正コミットだけを対象）

- 「1つ前の作業に戻す」の処理中に編集すると、戻した内容が保存されずに消えることがある
- 直前の自動保存に失敗していても入れ替え・ファイル読み込みが進み、古い版がバックアップされる（エラー表示も消える）
- バックアップが無いのにボタンが出ることがある
- 壊れた音声を含むプロジェクトファイルを読み込むたびに、一覧に出ない音素材が増える
- 書き込みの確定中に止まったトランザクションが終わらない可能性

→ すべて修正し、回帰テストを追加した（コミット `c9cf24ad`）。差分は自分でも読み直して確認した

---

## 7. 未対応・今後の課題

- **実機での確認**: 開発環境の制約で iPad 実機・iOS シミュレータでは確認できていない。特に次の点は実機で確認してほしい
  - iPadOS 14.5〜15 で、音あつめで録音した音を DAW に置いて再生できるか（Safari が自分の録音した mp4 をデコードできるか）
  - 消音スイッチ（マナーモード）オンで DAW の音が出るか
  - 縦向き・横向きでのタッチドラッグ
- **バックアップは 1 つだけ**: 「1つ前の作業に戻す」で戻せるのは直前の 1 つだけ。複数世代の履歴が必要なら今後の課題
- **iPad の容量**: 音声は base64 で保存しているため、実際のファイルより約 1.3 倍の容量を使う。音素材がとても多い場合は Blob のまま保存する形式への変更を検討（古い Safari は IndexedDB への Blob 保存に不具合があったため、今回は見送った）
- **API の URL が統一されていない**: `CloudPage` は `../api`、`AdminPage` と DAW のクラウド保存は `/api` を使っている。ルート（`/`）に置いた場合はどちらも `/api` になるので動くが、サブディレクトリに置くとどちらかが動かない。配置場所に合わせて統一を検討
- **追跡されている生成物**: `build/`、`coverage/`、`playwright-report/`、`test-results/` の一部が `.gitignore` 対象なのにコミットされている（今回は `node_modules` のみ外した）
- **`node_modules` の履歴**: 管理対象からは外したが、過去のコミットには残っているのでリポジトリの容量は減っていない。減らすには履歴の書き換え（force push）が必要
- **GitHub Pages 用の `docs/`**: 再ビルドしていない。公開する場合は `npm run deploy`（push まで行うので注意）
- **ホーム画面に追加した場合**: iPad でホーム画面に追加して使うと、Safari とは別の保存領域になる（Safari で作ったデータは見えない）。今回は `apple-mobile-web-app-capable` を付けていないので、ホーム画面のアイコンからも Safari で開く

---

## 8. デプロイ・移行時の注意

- 依存パッケージに `lucide-react@^0.460.0` を追加したので、デプロイ前に `npm install`（または `npm ci`）が必要
- `node_modules` を git から外したので、新しくクローンした環境では必ず `npm install` を実行する
- PHP は `api/audio_utils.php` を追加し、`api/uploads/audio/.htaccess` を追加した（Apache の場合。Nginx では無視されるので、必要なら同等の設定を入れる）
- 既存の端末のデータ（録音・DAW の作業内容）は、新しいバージョンを開いたときに自動で移行される。移行は元のデータを消す前に書き込みの確定を確認している

---

## 9. コミット一覧（新しい順）

| コミット | 内容 |
|:--|:--|
| `c9cf24ad` | fix: 最終確認で見つかった「1つ前の作業に戻す」まわりの問題を修正 |
| `4002c093` | fix: 2 周目のレビュー指摘（バックアップの復元・読み込み時の上書き・保存前の録音など）を修正 |
| `117bccf6` | fix: レビューで見つかったデータ消失と iPad 互換性の問題を修正 |
| `52c255e9` | fix: クラウド保存ダイアログの Esc 処理を document のリスナーに移し、lint 指摘を解消 |
| `be200eb1` | fix: 消音スイッチ対策が再生開始直後に解除されていた問題などレビュー指摘を修正 |
| `0d264ae9` | fix: 初回読み込み中の DAW 操作を止め、再生後に audioSession を元に戻す |
| `a94d8fd0` | fix(a11y): メニューの矢印キー移動を実装し、通っていなかった既存テストを実在の機能で書き直す |
| `6e2a422a` | feat(ui): アイコンを lucide-react に置き換え、使いにくい箇所を修正 |
| `4399c534` | fix: 録音形式に AAC を優先指定し、実ブラウザの E2E テストを追加 |
| `e0931374` | test: データ消失・iPad 互換性の回帰テストを追加（259件） |
| `cb49af68` | fix: ドラッグ座標が取れない場合の NaN 対策とタッチリスナーの取り付けを描画と同時に |
| `aa5e1f4e` | fix(api): アップロードの拡張子検証と Range 配信に対応 |
| `de479d97` | fix: ページ移動でタイムラインが消える問題などデータ消失・iPad不具合を修正 |
| `63ebf274` | fix: データ保存層と音声処理の共通ユーティリティを安定化 |
| `916c8b32` | chore: node_modules を git の管理対象から外す |
| （このファイル） | docs: 作業記録を追加 |
