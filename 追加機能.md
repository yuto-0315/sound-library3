# 追加機能案

## クラウド機能

- 録音した音声をローカルだけでなく、SQLデータベースにアップロードし、複数人で共有できるようにする
- アップロードされた音声を再生・検索し、必要に応じて自身の端末の「音ライブラリー」にダウンロード・登録できるようにする
- uidを使用することによって複数回のダウンロードを防ぐ。

## クラウドページの詳細

- 録音時に付与したタグや音声名などで検索可能にする
- データベース上に「部屋」（仮称）を作成し、ユーザーごとに利用する部屋を選択できるようにする
    - 初回利用時に部屋番号（数値入力・バリデーションあり）を入力
    - 入力した部屋番号はローカルストレージに保存し、次回以降も利用可能
- 先生が部屋を登録・管理できる管理ページを用意する

## 先生用管理ページの機能

- 「部屋」の新規作成（既存番号は登録不可）
- 生徒がアップロードした音声の一覧・再生
- 生徒が作成した曲を先生の端末で再現・再生
    - 生徒は任意のタイミングで曲データをサーバーに保存可能
    - その際に、自身の「班番号」を付加できるようにする

## 使用技術

- MySQLとPHPを使用
- ローカルでのテスト用にXAMPPでPHPとMySQLを利用

## 注意事項

- 機能名はすべて仮称です。より分かりやすい名称があれば適宜変更してください。
- UIや入力方法は小学生でも使いやすい設計を心がけてください。
- MySQLのDBを作成するためのSQLも出力すること