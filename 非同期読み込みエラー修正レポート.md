# 非同期読み込みエラー修正レポート

## 🐛 発生していた問題

### エラー内容
```
TypeError: Cannot read properties of undefined (reading 'map')
at DAWPage.js:2326:21
```

### 原因
`loadAutoSavedProject()`を`async`関数に変更したため、戻り値がPromiseオブジェクトになり、`useState`の初期値として使用できなくなっていました。

**問題のコード:**
```javascript
const loadAutoSavedProject = async () => {
  // ...非同期処理
  return { tracks: [...], pixelsPerSecond: ... };
};

const initialData = loadAutoSavedProject(); // Promiseが返される!
const [tracks, setTracks] = useState(initialData.tracks); // undefined.tracksでエラー
```

---

## ✅ 実施した修正

### 修正方針
`useState`の初期値は同期的でなければならないため、以下のように変更:

1. **初期値はデフォルト値を設定**
2. **非同期読み込みは`useEffect`で実行**
3. **読み込み完了後に`setState`で更新**

### 修正内容

#### Before (問題のあるコード)
```javascript
const DAWPage = () => {
  const loadAutoSavedProject = async () => {
    // ...非同期処理
    return {
      tracks: [...],
      pixelsPerSecond: ...
    };
  };

  const initialData = loadAutoSavedProject(); // Promise!
  const [tracks, setTracks] = useState(initialData.tracks); // エラー!
  const [pixelsPerSecond, setPixelsPerSecond] = useState(DEFAULT_PIXELS_PER_SECOND);
  // ...他のstate
```

#### After (修正後)
```javascript
const DAWPage = () => {
  // 初期値はデフォルト値を使用
  const [tracks, setTracks] = useState([{ 
    id: Date.now(), 
    name: 'トラック 1', 
    clips: [] 
  }]);
  const [pixelsPerSecond, setPixelsPerSecond] = useState(DEFAULT_PIXELS_PER_SECOND);
  // ...他のstate
  
  // 非同期読み込みはuseEffectで実行
  useEffect(() => {
    const loadAutoSavedProject = async () => {
      try {
        const autoSavedData = localStorage.getItem('dawProjectAutoSave');
        if (autoSavedData) {
          const projectData = JSON.parse(autoSavedData);
          
          // トラックカウンターの復元
          if (projectData.trackNameCounter) {
            trackNameCounterRef.current = projectData.trackNameCounter;
          }
          if (projectData.trackIdCounter) {
            trackIdCounterRef.current = projectData.trackIdCounter;
          }
          
          // ピクセル/秒の復元
          if (projectData.pixelsPerSecond) {
            setPixelsPerSecond(projectData.pixelsPerSecond);
          }
          
          // IndexedDBから音素材を取得
          const savedSounds = await getAllRecordings();
          const soundsMap = new Map();
          
          savedSounds.forEach(sound => {
            if (sound.name && sound.audioData) {
              soundsMap.set(sound.name, sound);
            }
          });
          
          // audioBlobを復元
          const restoreAudioBlob = (soundData) => {
            // ...復元ロジック
          };
          
          // トラックを復元
          const validTracks = (projectData.tracks || []).map(track => ({
            ...track,
            clips: (track.clips || [])
              .map(clip => {
                // ...クリップ復元ロジック
              })
              .filter(clip => clip !== null)
          }));
          
          // stateを更新
          if (validTracks.length > 0) {
            setTracks(validTracks);
            console.log('✓ 自動保存データを復元しました');
          }
        }
      } catch (error) {
        console.error('自動保存データの復元に失敗:', error);
      }
    };
    
    loadAutoSavedProject();
  }, []);
```

### 追加修正

**重複宣言の削除:**
```javascript
// Line 127: 削除
// const [pixelsPerSecond, setPixelsPerSecond] = useState(DEFAULT_PIXELS_PER_SECOND);

// コメントで説明を追加
// pixelsPerSecondは上で宣言済み
```

---

## 📊 修正の影響

### ビルド結果
```
✓ Compiled with warnings
File sizes after gzip:
  79.89 kB (+28 B)  build/static/js/main.76c5eca8.js
  10.87 kB          build/static/css/main.2373a28a.css
```

### 動作フロー

#### 修正前 (エラー)
```
1. コンポーネント初期化
   ↓
2. loadAutoSavedProject()実行 → Promise返却
   ↓
3. useState(Promise.tracks) → undefined
   ↓
4. レンダリング時に tracks.map() → エラー!
```

#### 修正後 (正常)
```
1. コンポーネント初期化
   ↓
2. useState(デフォルト値) → 空のトラック配列
   ↓
3. レンダリング成功 (デフォルトUIを表示)
   ↓
4. useEffect実行
   ↓
5. 非同期でデータ読み込み
   ↓
6. setTracks()で更新
   ↓
7. 再レンダリング (復元されたデータを表示)
```

---

## 🎯 メリット

### ユーザー体験の向上
```
Before: ローディング中にエラー → 画面が表示されない
After:  即座にデフォルトUIを表示 → データ読み込み後に更新
```

### エラーハンドリング
```javascript
try {
  const savedSounds = await getAllRecordings();
  // ...処理
} catch (error) {
  console.error('自動保存データの復元に失敗:', error);
  // デフォルトのtrackは既に表示されているので、
  // エラーが発生してもアプリは使用可能
}
```

### パフォーマンス
- 初期レンダリングがブロックされない
- IndexedDBの読み込み中もUIが応答する
- 段階的なデータ読み込みが可能

---

## 🔄 データ読み込みフロー

### 1. 初期表示 (即座)
```javascript
tracks = [{ 
  id: Date.now(), 
  name: 'トラック 1', 
  clips: [] 
}]
```

### 2. 自動保存データの確認
```javascript
const autoSavedData = localStorage.getItem('dawProjectAutoSave');
if (autoSavedData) {
  // データがあれば復元処理を開始
}
```

### 3. IndexedDBから音素材を取得
```javascript
const savedSounds = await getAllRecordings();
// localStorageからの自動移行も含む
```

### 4. トラックとクリップを復元
```javascript
const validTracks = projectData.tracks.map(track => ({
  ...track,
  clips: track.clips.map(clip => ({
    ...clip,
    soundData: restoreAudioBlob(clip.soundData)
  }))
}));
```

### 5. stateを更新
```javascript
setTracks(validTracks);
setPixelsPerSecond(projectData.pixelsPerSecond);
```

---

## 🧪 テスト項目

### 基本動作
- [x] 初回起動時にデフォルトトラックが表示される
- [x] 自動保存データがある場合、正常に復元される
- [x] トラック操作が正常に動作する
- [x] ズーム機能が正常に動作する

### エッジケース
- [x] 自動保存データがない場合
- [x] 自動保存データが壊れている場合
- [x] IndexedDB読み込みエラー時
- [x] 音素材が見つからない場合

### パフォーマンス
- [x] 初期表示が即座に行われる
- [x] 大量のトラック/クリップの復元
- [x] ページリロード時の動作

---

## 📝 学んだ教訓

### React Hooksの原則

1. **useStateの初期値は同期的でなければならない**
   ```javascript
   ❌ useState(asyncFunction()) // Promise
   ✅ useState(defaultValue)     // 実際の値
   ```

2. **非同期処理はuseEffectで実行**
   ```javascript
   useEffect(() => {
     const loadData = async () => {
       const data = await fetchData();
       setState(data);
     };
     loadData();
   }, []);
   ```

3. **段階的なデータ読み込み**
   - まずデフォルト値で表示
   - 非同期でデータ読み込み
   - 読み込み完了後に更新

---

## 🔧 今後の改善案

### ローディング状態の追加
```javascript
const [isLoading, setIsLoading] = useState(true);

useEffect(() => {
  const loadAutoSavedProject = async () => {
    try {
      // ...読み込み処理
    } finally {
      setIsLoading(false);
    }
  };
  loadAutoSavedProject();
}, []);

// UIで表示
{isLoading && <div className="loading">読み込み中...</div>}
```

### エラー状態の管理
```javascript
const [loadError, setLoadError] = useState(null);

// エラー時の表示
{loadError && (
  <div className="error">
    データの読み込みに失敗しました: {loadError.message}
  </div>
)}
```

### プログレス表示
```javascript
const [loadProgress, setLoadProgress] = useState(0);

// 段階的に進捗を更新
setLoadProgress(25); // プロジェクトデータ読み込み
setLoadProgress(50); // 音素材取得
setLoadProgress(75); // トラック復元
setLoadProgress(100); // 完了
```

---

## 🎉 まとめ

**修正箇所:** 2箇所
- useState初期化処理
- 重複宣言の削除

**解決したエラー:**
- ✅ TypeError: Cannot read properties of undefined

**改善点:**
- ✅ 初期表示の高速化
- ✅ エラー耐性の向上
- ✅ ユーザー体験の改善

Reactの非同期処理パターンに従い、安定した読み込み処理を実現しました。

---

作成日: 2025年10月16日
