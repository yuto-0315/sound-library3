# アップロードされたファイルへの直接アクセスを禁止する（配信は download.php 経由で行う）。
# 万一スクリプトのようなファイルが置かれても実行されないようにするため。
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
